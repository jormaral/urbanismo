package com.mitc.redes.editorfip.servicios.mapas;

import java.util.List;

import javax.ejb.Local;

import com.mitc.redes.editorfip.servicios.mapas.EntidadMapaDTO;	

@Local
public interface ServicioEntidadMapa {
	
	/**
	 * Obtiene una lista con todas las entidades pertenecientes al trámite que le pasamos
	 *  y cuyo grupo de Entidades corresponde al parámetro codigo
	 * 
	 * @param idEntidad ID de la entidad sobre la cual se va a operar.
	 * @return lista de entidades en formato EntidadMapaDTO
	 */
	public List<EntidadMapaDTO> listadoEntidadesColorMapa (int idtramite, String codigo);

	
	

}
